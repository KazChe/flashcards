import { useState } from "react";

export default function FlashcardSearchFilter() {
    const [category, setCategory] = useState(setCategory)
    const [tag, setTag] = useState(setTag)

    return (
        <div>
            
        </div>
    )
}